<Window x:Class="NDL.AutoHangerTool.UI.AutoHangerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="NDL Auto Hanger &amp; Insert Placement" 
        Height="530" Width="450"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#1E1E1E"
        Foreground="#E0E0E0"
        FontFamily="Segoe UI">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Text="⚡ AUTO HANGER &amp; INSERT PLACEMENT" FontSize="16" FontWeight="Bold" Foreground="#4EC9B0"/>
            <TextBlock Text="Automatically calculates rod length to slab bottom and displays 2D/3D symbols." FontSize="11" Foreground="#A0A0A0" Margin="0,4,0,0" TextWrapping="Wrap"/>
        </StackPanel>

        <!-- Main Form -->
        <Border Grid.Row="1" Background="#252526" CornerRadius="6" Padding="14" BorderBrush="#3E3E42" BorderThickness="1">
            <StackPanel>
                <!-- Pipe Selection Info -->
                <Border Background="#2D2D30" CornerRadius="4" Padding="10" Margin="0,0,0,10">
                    <TextBlock x:Name="txtSelectedInfo" Text="Selected: 0 pipe segments" FontWeight="SemiBold" Foreground="#007ACC"/>
                </Border>

                <!-- Select Family -->
                <TextBlock Text="Hanger / Attachment Family:" Foreground="#CCCCCC" Margin="0,0,0,4"/>
                <ComboBox x:Name="cboHangerFamily" Margin="0,0,0,10" Background="#333337" Foreground="Black" Padding="4"/>

                <!-- Spacing -->
                <Grid Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="110"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Hanger Spacing (inches):" VerticalAlignment="Center" Foreground="#CCCCCC"/>
                    <TextBox x:Name="txtSpacing" Grid.Column="1" Text="96" Background="#333337" Foreground="White" BorderBrush="#555" Padding="4" HorizontalContentAlignment="Center"/>
                </Grid>

                <!-- Near Fittings Checkbox -->
                <CheckBox x:Name="chkNearFittings" Content="Place hangers near Elbows / Tees:" IsChecked="True" Foreground="#CCCCCC" Margin="0,2,0,4"/>
                <Grid Margin="20,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="110"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Fitting Offset (inches):" VerticalAlignment="Center" Foreground="#A0A0A0" FontSize="11"/>
                    <TextBox x:Name="txtFittingOffset" Grid.Column="1" Text="12" Background="#333337" Foreground="White" BorderBrush="#555" Padding="4" HorizontalContentAlignment="Center"/>
                </Grid>

                <!-- Rod Size -->
                <Grid Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="110"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Threaded Rod Size:" VerticalAlignment="Center" Foreground="#CCCCCC"/>
                    <ComboBox x:Name="cboRodSize" Grid.Column="1" SelectedIndex="1" Background="#333337" Foreground="Black">
                        <ComboBoxItem Content="3/8&quot;"/>
                        <ComboBoxItem Content="1/2&quot;"/>
                        <ComboBoxItem Content="5/8&quot;"/>
                        <ComboBoxItem Content="3/4&quot;"/>
                        <ComboBoxItem Content="7/8&quot;"/>
                        <ComboBoxItem Content="1&quot;"/>
                        <ComboBoxItem Content="M10"/>
                        <ComboBoxItem Content="M12"/>
                    </ComboBox>
                </Grid>

                <!-- Default Slab Height -->
                <Grid Margin="0,0,0,4">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="110"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Default Slab Height (inches):" VerticalAlignment="Center" Foreground="#CCCCCC"/>
                    <TextBox x:Name="txtSlabHeight" Grid.Column="1" Text="144" Background="#333337" Foreground="White" BorderBrush="#555" Padding="4" HorizontalContentAlignment="Center"/>
                </Grid>
                <TextBlock Text="* Auto-detects beam and floor slabs from linked models (Revit Link)." FontSize="10" Foreground="#6A9955" Margin="0,2,0,6"/>

                <!-- Generate Family Button -->
                <Button x:Name="btnCreateFamily" Content="📦 Auto-Generate / Reload NDL Hanger Family" Height="26" Background="#333337" Foreground="#4EC9B0" BorderBrush="#555" BorderThickness="1" Cursor="Hand" Click="BtnCreateFamily_Click"/>
            </StackPanel>
        </Border>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button x:Name="btnCancel" Content="Cancel" Width="80" Height="32" Background="#3E3E42" Foreground="White" BorderThickness="0" Margin="0,0,10,0" Click="BtnCancel_Click"/>
            <Button x:Name="btnPlace" Content="⚡ PLACE HANGERS" Width="160" Height="32" Background="#007ACC" Foreground="White" FontWeight="Bold" BorderThickness="0" Click="BtnPlace_Click"/>
        </StackPanel>
    </Grid>
</Window>
