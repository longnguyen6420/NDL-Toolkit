<Window x:Class="OffsetPipeTool.UI.OffsetPipeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Offset Pipes Perpendicularly" Height="220" Width="360"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="#F4F4F5" Foreground="#18181B" Topmost="True"
        FontFamily="Segoe UI" FontSize="13">

    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Selected Items Info -->
        <TextBlock Grid.Row="0" x:Name="lblInfo" Text="Đã chọn: 0 đối tượng đường ống" 
                   FontWeight="SemiBold" Foreground="#2563EB" Margin="0,0,0,12"/>

        <!-- Distance Field -->
        <Grid Grid.Row="1" Margin="0,4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="130"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Khoảng cách dịch" VerticalAlignment="Center"/>
            <TextBox Grid.Column="1" x:Name="txtDistance" Text="9 6" Padding="6,4" 
                     ToolTip="Cú pháp linh hoạt: Feet Inch ('9 6'), Inch ('4' hoặc '1 1/2'), hoặc mm ('100')"/>
        </Grid>

        <!-- Syntax Helper Note -->
        <TextBlock Grid.Row="2" Text="* Nhập '9 6' (9ft6in), '4' (4 in), '1 1/2' (1.5 in) hoặc '100' (100mm)" 
                   FontSize="11" Foreground="#71717A" Margin="0,6,0,0"/>

        <!-- Action Buttons -->
        <Grid Grid.Row="3" VerticalAlignment="Bottom" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="Cancel" Click="BtnCancel_Click" Height="32" 
                    Background="#E4E4E7" Foreground="#18181B" BorderThickness="1" BorderBrush="#D4D4D8" Cursor="Hand"/>
            <Button Grid.Column="2" Content="OK" Click="BtnOK_Click" Height="32" 
                    Background="#2563EB" Foreground="#FFFFFF" FontWeight="Bold" BorderThickness="0" Cursor="Hand"/>
        </Grid>
    </Grid>
</Window>
